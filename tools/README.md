# Tools 工具集

本目录包含项目的各种工具。

## 📁 目录结构

```
tools/
├── filelist-generator/        # 文件列表生成和解析工具
│   ├── generate-filelist.py   # 生成器
│   ├── filelist_parser.py     # 解析器
│   ├── api_example.py          # API 示例
│   └── README.md               # 详细文档
│
├── sync-static-files/           # 文件监视和同步工具
│   ├── watch-and-sync.py       # 监视和同步脚本
│   ├── start-watch.sh          # 启动脚本
│   ├── test-watch.py           # 配置测试
│   └── README.md               # 详细文档
│
└── create-backdrops-blur-image/ # 背景图片模糊工具
    ├── main.py                  # 主程序
    └── README.md                # 使用说明
```

## 🛠️ 工具说明

### 1. 文件列表生成器 (filelist-generator)

生成和解析静态资源文件列表。

**功能**：
- 扫描 static/ 目录生成文件列表
- 提供 Python 解析库供 server 端使用
- 支持分页、过滤、搜索等功能

**使用**：
```bash
# 生成文件列表
./generate-filelist.sh business-headshot-ai

# 查看文档
cat tools/filelist-generator/README.md
```

### 2. 文件监视和同步 (sync-static-files)

自动监视 static/ 目录变化并同步到 server 端。

**功能**：
- 监视文件变化
- 自动生成文件列表
- 自动同步到 server 端
- 防抖动机制

**使用**：
```bash
# 安装依赖
pip3 install watchdog

# 启动监视
./start-watch.sh

# 查看文档
cat tools/sync-static-files/README.md
```

### 3. 背景图片模糊工具 (create-backdrops-blur-image)

为背景图片生成不同模糊级别的版本。

**功能**：
- 生成多个模糊级别（0, 5, 10, 15）
- 批量处理
- 自动优化

**使用**：
```bash
cd tools/create-backdrops-blur-image
python main.py

# 查看文档
cat tools/create-backdrops-blur-image/README.md
```

## 🚀 快速开始

### 开发环境完整设置

```bash
# 1. 生成文件列表
./generate-filelist.sh business-headshot-ai

# 2. 启动静态文件服务器
python3 dev_server.py &

# 3. 启动文件监视（可选）
./start-watch.sh &

# 4. 启动 server 端
cd ../aipso-server && npm run dev
```

## 📚 相关文档

- [文件列表工具指南](./filelist-generator/README.md)
- [文件监视和同步](./sync-static-files/README.md)
- [开发工作流程](./sync-static-files/DEVELOPMENT_WORKFLOW.md)
- [背景图片工具](./create-backdrops-blur-image/README.md)

## 💡 推荐工作流程

### 日常开发

1. 启动文件监视工具（自动化）
2. 添加/修改图片
3. 自动生成和同步

### 批量导入

1. 启动文件监视工具
2. 批量复制图片
3. 自动处理（防抖动合并）

### 手动操作

1. 添加/修改图片
2. 运行生成脚本
3. 手动复制到 server 端

## 🔧 依赖安装

```bash
# 文件监视工具依赖
pip3 install watchdog

# 背景图片工具依赖
pip3 install Pillow
```

## 📝 注意事项

1. **文件列表位置**：生成在 `tools/filelist-generator/{product}/files.txt`
2. **不提交到 git**：文件列表已添加到 `.gitignore`
3. **Server 端同步**：监视工具会自动同步到 server 端
4. **防抖动**：2秒内同一产品只触发一次处理

## 🎯 工具选择

| 场景 | 推荐工具 |
|------|---------|
| 频繁更新图片 | 文件监视工具 |
| 偶尔更新图片 | 手动生成脚本 |
| 批量导入图片 | 文件监视工具 |
| 生成背景图片 | 背景图片工具 |
| Server 端集成 | 文件列表解析器 |

## 🎉 总结

这些工具提供了完整的静态资源管理解决方案：

- ✅ 自动化文件列表生成
- ✅ 实时监视和同步
- ✅ 灵活的解析和查询
- ✅ 批量图片处理

让开发流程更加高效！🚀
